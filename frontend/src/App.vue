<template>
  <div id="app">
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <a class="navbar-brand" href="#">App</a>
        <div class="collapse navbar-collapse">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <a class="nav-link" href="#" @click.prevent="showAddItem">Ajouter un Item</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#" @click.prevent="showItemList">Liste des Items</a>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <div class="container">
      <AddItem v-if="view === 'add'" />
      <ItemList v-if="view === 'list'" />
    </div>
  </div>
</template>

<script>
import AddItem from './components/AddItem.vue';
import ItemList from './components/ItemList.vue';

export default {
  data() {
    return {
      view: 'add'
    };
  },
  components: {
    AddItem,
    ItemList
  },
  methods: {
    showAddItem() {
      this.view = 'add';
    },
    showItemList() {
      this.view = 'list';
    }
  }
};
</script>
